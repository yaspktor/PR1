#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>


printf("Podaj date: ");
	scanf("%[^\n]", input);
	if (sscanf(input, "%4d/%2d/%2d", &rok, &miesiac, &dzien) == 3){
		printf("ok\n");
		printf("%d - %d - %d\n", dzien, miesiac, rok);
	}

    printf("Wynik: %.2f\n", wynik);

    while (scanf("%d" , &input) && input >= 0)

    atoi(argv[1]);
    arr = (char*) malloc (sizeof(char));
    arr = (char*) realloc (arr, i * sizeof(char));

    matrix = (double **) malloc(n * sizeof(double*));
	for (i = 0; i < n; i++) matrix[i] = (double*) malloc(m* sizeof(double));
     for (int i = 0; i < r; i++)
        free(arr[i])

        (plik1 = fopen(argv[1], "r")) == NULL 
        rewind(plik2);
        while ( (c=getc(in)) != EOF)
        #define SWAP(t, x, y) {t temp = x; x = y; y = temp; }
        fscanf(plik, "%d", &studenci[i].punkty);
        putc(c, out);/*fprintf(out, "%c",c);*/
        /*printf("%c",c);*/
		putc(c, stdout);

        void wykonaj(Odcinek* (*fun)(Odcinek*, int), Odcinek *x, int n);

        void (*fun_ptr)(int) = &fun;